---
import { SEO } from 'astro-seo'

export interface Props {
  title?: string
}

const { title } = Astro.props
const builtTitle = title ? `${title} · Creatorsgarten` : 'Creatorsgarten'
---

<SEO
  slot="head"
  title={builtTitle}
  openGraph={{
    basic: {
      title: builtTitle,
      type: "website",
      image: Astro.url.origin + "/images/og.jpg",
    }
  }}
/>
