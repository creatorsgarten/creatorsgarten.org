---
export interface Props {
  bold?: string
  normal?: string
}

const { bold, normal } = Astro.props
---

{bold && <span class="font-medium">{bold}</span>}
{bold && normal && <span class="mx-1">|</span>}
{normal && <span>{normal}</span>}
